import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    gap: 7,
    alignItems: "center",
    flexDirection: "row",
  },
  description: {
    flex: 1,
    fontSize: 14,
    fontWeight: 600,
  },
})
