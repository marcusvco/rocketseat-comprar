import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    gap: 5,
    alignItems: "center",
    flexDirection: "row",
  },
  title: {
    fontSize: 12,
    fontWeight: 600,
  },
})
